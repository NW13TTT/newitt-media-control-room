import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const headers={"Content-Type":"application/json","Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"}; const reply=(body:Record<string,unknown>,status=200)=>new Response(JSON.stringify(body),{status,headers});
Deno.serve(async request=>{if(request.method==="OPTIONS")return new Response("ok",{headers});if(request.method!=="POST")return reply({error:"Method not allowed."},405);const authorization=request.headers.get("Authorization"),url=Deno.env.get("SUPABASE_URL"),anon=Deno.env.get("SUPABASE_ANON_KEY"),service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!authorization?.startsWith("Bearer ")||!url||!anon||!service)return reply({error:"Service unavailable."},503);const caller=createClient(url,anon,{global:{headers:{Authorization:authorization}}});const {data}=await caller.auth.getUser();if(!data.user)return reply({error:"Authentication is required."},401);const admin=createClient(url,service,{auth:{autoRefreshToken:false,persistSession:false}});const {data:profile}=await admin.from("profiles").select("id").eq("id",data.user.id).eq("role","MASTER_ADMIN").maybeSingle();if(!profile)return reply({error:"Master Admin access is required."},403);const configured=Boolean(Deno.env.get("CLOUDFLARE_API_TOKEN")&&Deno.env.get("CLOUDFLARE_ACCOUNT_ID"));return reply({status:configured?"configured":"not_configured",message:configured?"Cloudflare configuration is available.":"Cloudflare App is not configured."});});
